"""TurnRD (Method B) — learned per-turn reward decomposer.

Surface (M1):
- `TurnRDConfig`, `TurnRDOutput`, `TurnRD` from `src.turnrd.model`.

Day 13–14 will add `dataset.py` (replay buffer reader) and `train.py`
(standalone trainer entrypoint), plus the production policy-hidden-state
embedder. See `MEDIUM_FIXES.md::M1` and the
`~/.llms/plans/cs224r_hgpo_method_b_turnrd_m1.plan.md` plan file.
"""
