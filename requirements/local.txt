# Local dev dependencies (macOS arm64-friendly).
#
# These run on your laptop for tests, lint, types, and Modal CLI.
# Heavy ML deps (torch, transformers, peft, vllm, trl) are NOT here — they
# only run inside the Modal image (see requirements/modal.txt).

# --- test + lint toolchain ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
pyright>=1.1.380

# --- Modal client (orchestrates remote runs) ---
modal>=0.64

# --- Lightweight clients we want available locally for unit tests ---
openai>=1.40
httpx>=0.27
pydantic>=2.6

# --- Useful, no native deps ---
PyYAML>=6.0
tqdm>=4.66
